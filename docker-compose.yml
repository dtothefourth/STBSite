version: '3.8'

services:
 php:
  image: php:8.3-fpm
  container_name: simplephp-php
  volumes:
  - php-assets:/var/www/html

 nginx:
  image: nginx:alpine
  ports:
  - "8080:80"
  container_name: simplephp-nginx
  volumes:
  - php-assets:/var/www/html
  - ./nginx-conf:/etc/nginx/conf.d
  depends_on:
  - php

volumes:
  # Define the shared volume
  php-assets: